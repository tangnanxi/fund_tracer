# Fund Tracker Skill

查询订阅基金的实时估算净值、涨跌幅与持仓收益。

## 数据来源

- 天天基金网（East Money）公开接口：`https://fundgz.1234567.com.cn/js/{code}.js`
- 返回单只基金的实时估算净值（`gsz`）、估算涨跌幅（`gszzl`）、估值时间（`gztime`）等。
- 仅在中国大陆交易时段内有实时估算数据；非交易时段可能返回上一交易日收盘净值。

## 文件说明

| 文件 | 说明 |
| --- | --- |
| `SKILL.md` | 本说明文档 |
| `fund_tracker.py` | 查询与收益计算脚本 |
| `funds.txt` | 基金代码列表（每行一个） |
| `holdings.json` | 持仓配置：份额、成本价 |
| `history.json` | 每日估算净值历史 |

## 使用方法

### 1. 配置基金代码

编辑 `funds.txt`，把你订阅的基金代码写进去，每行一个：

```text
008114
166002
002009
```

也可以带备注（脚本只读取每行第一个空白符前的内容）：

```text
001632 天弘食品饮料
005827 易方达蓝筹
```

### 2. 首次运行生成持仓配置

```bash
python fund_tracker.py
```

首次运行会自动在同目录下生成 `holdings.json`，格式如下：

```json
{
  "holdings": [
    {
      "code": "008114",
      "shares": 0.0,
      "cost_price": 1.6401
    }
  ]
}
```

### 3. 填入真实份额与成本价

打开 `holdings.json`，把每只基金的 `shares`（持有份额）和 `cost_price`（买入单价）改成你的真实数据：

```json
{
  "holdings": [
    {
      "code": "008114",
      "shares": 1000.0,
      "cost_price": 1.6000
    }
  ]
}
```

- `shares`：你持有的基金份额
- `cost_price`：你的买入成本价（用于计算累计收益）

如果暂时不想计算收益，可以把 `shares` 保持为 `0`。

### 4. 每日同步

再次运行脚本：

```bash
python fund_tracker.py
```

脚本会：

1. 读取 `funds.txt` 中的基金代码
2. 查询每只基金的实时估算净值与涨跌幅
3. 根据 `holdings.json` 中的份额与成本价计算：
   - **当日预估收益** = 昨日净值 × 份额 × 估算涨跌幅
   - **持仓市值** = 估算净值 × 份额
   - **累计收益** = （估算净值 − 成本价）× 份额
4. 将当日净值追加到 `history.json`（同一天不会重复写入）

### 5. 在 Kimi 中调用

直接对我说：

> “查一下我的基金”

我会读取 `~/.kimi-code/skills/fund-tracker/` 下的配置并返回实时涨跌与收益。

## 输出示例

```text
查询时间: 2026-06-17 22:33:16
共 3 只基金

基金代码  基金名称                        份额        最新净值  估算净值  估算涨跌  当日预估收益  持仓市值  累计收益  估值时间
----------------------------------------------------------------------------------------------------
008114    天弘中证红利低波动100联接A    1000.00     1.6522    1.6401   -0.73%      -12.06   1640.10    40.10  2026-06-17 15:00
166002    中欧新蓝筹混合A               500.00      3.5731    3.6083   +0.99%      +17.67   1804.15    54.15  2026-06-17 15:00
002009    中欧瑾通灵活配置混合A        2000.00     1.5459    1.5475   +0.11%        3.32   3095.00    32.00  2026-06-17 15:00
----------------------------------------------------------------------------------------------------
总市值: 6539.25
当日总预估收益: +8.93
累计总收益: +126.25
```

## 注意事项

- 接口返回的是**估算净值**，可能与当晚发布的实际净值有偏差。
- 若基金已停牌或不在交易时段，`gszzl` 可能为 `0` 或空，当日预估收益会显示为 `0.00`。
- 网络不稳定时可能请求失败，可稍后重试。
- 首次生成的 `cost_price` 默认取当天估算净值，**请务必改为真实成本价**后再用于决策。
